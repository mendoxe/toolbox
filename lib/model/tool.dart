import 'package:flutter/material.dart';

class Tool {
  IconData toolIcon;
  String title;
  MaterialColor color;
  String route;
  Tool({
    required this.toolIcon,
    required this.title,
    required this.color,
    required this.route,
  });
}
